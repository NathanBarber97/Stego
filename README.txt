To use the program execute the following instructions:

Build the makefile first to download the necessary packages
The program is run as such "./dcutils.py"
Once the program has loaded you can begin
Use the "Get" buttons to load the desired images, type in a password to add security to the encryption
Use the Encrypt and Decrypt buttons to hide or reveal images
